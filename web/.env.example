# Web Frontend Development Configuration
# Use this when running 'npm run dev' locally (without Docker)

# API Base URL (Vite requires VITE_ prefix for client-side access)
# For local API development:
VITE_API_URL=http://localhost:8080

# For Docker compose dev mode (API on different port):
# VITE_API_URL=http://localhost:8080

# For production with proxy:
# VITE_API_URL=http://localhost
# or
# VITE_API_URL=https://your-domain.com
